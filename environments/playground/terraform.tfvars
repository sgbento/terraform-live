aws_region = "eu-west-1"

# Your home IP — find it by running: curl https://checkip.amazonaws.com
# Then add /32 at the end, e.g. "1.2.3.4/32"
my_ip = "YOUR_IP/32"

# Name of your key pair in AWS (create it in AWS Console → EC2 → Key Pairs)
key_pair_name = "YOUR_KEY_PAIR_NAME"
